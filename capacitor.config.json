import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.yourcompany.automanagevm',
  appName: 'AutoManageVM',
  webDir: 'www',
  server: {
    url: 'https://automanagevm.onhercules.app', // <--- Your hosted live site
    cleartext: false
  }
};

export default config;
